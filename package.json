{
  "name": "okf-postal-db",
  "version": "0.1.0",
  "description": "DB con CRUD para agentes: estado materializado en OKF (Markdown+YAML) y bitácora firmada append-only estilo postal, expuesta vía REST.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "init-identity": "node src/identity.js init",
    "verify": "node src/cli.js verify",
    "test": "node --test \"test/**/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fastify": "^4.28.1",
    "gray-matter": "^4.0.3"
  }
}
